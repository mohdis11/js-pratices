// A normal comparison in every lauguage i.e, a basics comparison we always do

console.log(2 == 2);
console.log(2 <= 1);
console.log(2 < 1);
console.log(2 >= 1);
console.log(2 > 1);
console.log(2 != 1);


// some comparison that actually Mainly in JS 

console.log("1" < 2);
console.log(3 > "2");

// similar operation we ==, !=, >=, <= we get the actual answer we needed where the above line give the out we need 

console.log(null > 0);
console.log(null == 0);
console.log(null >= 0);
console.log(null != 0);
console.log(null < 0);
console.log(null <= 0);

console.log(undefined < 0);
console.log(undefined <= 0);
console.log(undefined == 0);
console.log(undefined >= 0);
console.log(undefined > 0);
console.log(undefined != 0);

// useage a the above 12 lines we need avoid if want the code to be clean


console.log(2 === "2");

//if we use "==" we just compare the value and if we use  "===" then we are going to compare the value with datatype 
